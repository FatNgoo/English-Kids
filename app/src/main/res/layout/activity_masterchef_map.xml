<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/map_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="@drawable/bg_masterchef_map">

    <!-- Parallax Background Layers -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        
        <!-- Back layer (slowest) - Kitchen pattern -->
        <ImageView
            android:id="@+id/bg_layer_back"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_masterchef_map"
            android:alpha="0.6" />
        
        <!-- Middle layer - Subtle overlay -->
        <ImageView
            android:id="@+id/bg_layer_mid"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/bg_masterchef_map"
            android:alpha="0.3" />
    </FrameLayout>

    <!-- Level Nodes ScrollView -->
    <HorizontalScrollView
        android:id="@+id/map_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"
        android:fillViewport="true">

        <LinearLayout
            android:id="@+id/level_nodes_container"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:padding="32dp"
            android:gravity="center_vertical">
            
            <!-- Level nodes will be added programmatically -->
            
        </LinearLayout>

    </HorizontalScrollView>

    <!-- Toolbar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/transparent"
        app:navigationIcon="@drawable/ic_back"
        app:title="Master Chef Map"
        app:titleTextColor="@color/white"
        android:elevation="4dp" />

    <!-- Cookbook FAB -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_cookbook"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:contentDescription="@string/cookbook"
        app:srcCompat="@drawable/ic_book"
        app:tint="@color/white" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
