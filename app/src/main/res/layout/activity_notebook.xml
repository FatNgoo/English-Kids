<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_pink">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Top Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="16dp"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="@drawable/bg_button_circle"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:contentDescription="Back" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="16dp"
                android:text="📔 Sổ tay Ma thuật"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF" />

            <TextView
                android:id="@+id/txtCollectionCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Bộ sưu tập: 0 từ vựng"
                android:textSize="14sp"
                android:textColor="#FFFFFF" />
        </LinearLayout>

        <!-- Filter Buttons -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="none"
            android:paddingHorizontal="16dp">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btnFilterAll"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:background="@drawable/bg_button_secondary"
                    android:text="Tất cả"
                    android:textColor="#FFFFFF"
                    android:paddingHorizontal="16dp"
                    android:textSize="12sp" />

                <Button
                    android:id="@+id/btnFilterNature"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_card_green"
                    android:text="🌿 Thiên nhiên"
                    android:textColor="#FFFFFF"
                    android:paddingHorizontal="16dp"
                    android:textSize="12sp" />

                <Button
                    android:id="@+id/btnFilterFantasy"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_card_purple"
                    android:text="✨ Huyền bí"
                    android:textColor="#FFFFFF"
                    android:paddingHorizontal="16dp"
                    android:textSize="12sp" />

                <Button
                    android:id="@+id/btnFilterScifi"
                    android:layout_width="wrap_content"
                    android:layout_height="40dp"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_card_cyan"
                    android:text="🚀 Sci-Fi"
                    android:textColor="#FFFFFF"
                    android:paddingHorizontal="16dp"
                    android:textSize="12sp" />
            </LinearLayout>
        </HorizontalScrollView>

        <!-- Empty Message -->
        <TextView
            android:id="@+id/txtEmptyMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="32dp"
            android:gravity="center"
            android:text="Chưa có từ vựng nào!\nHãy chơi các màn để thu thập 🎵"
            android:textSize="18sp"
            android:textColor="#CCCCCC"
            android:visibility="gone" />

        <!-- Collection Grid -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewCollection"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:padding="16dp"
            android:clipToPadding="false"
            tools:listitem="@layout/item_collection" />

    </LinearLayout>

    <!-- Detail Panel Overlay -->
    <FrameLayout
        android:id="@+id/detailPanel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#AA000000"
        android:visibility="gone">

        <androidx.cardview.widget.CardView
            android:id="@+id/cardDetail"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:cardCornerRadius="16dp"
            app:cardElevation="12dp"
            app:cardBackgroundColor="#FFFFFF">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="24dp"
                android:gravity="center">

                <!-- Vocab -->
                <TextView
                    android:id="@+id/txtDetailVocab"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="APPLE"
                    android:textSize="32sp"
                    android:textStyle="bold"
                    android:textColor="#333333" />

                <!-- Vietnamese -->
                <TextView
                    android:id="@+id/txtDetailVietnamese"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="Quả táo"
                    android:textSize="20sp"
                    android:textColor="#666666" />

                <!-- Theme -->
                <TextView
                    android:id="@+id/txtDetailTheme"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="Chủ đề: NATURE"
                    android:textSize="14sp"
                    android:textColor="#888888" />

                <!-- Buttons -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginTop="24dp">

                    <ImageButton
                        android:id="@+id/btnPlayVocab"
                        android:layout_width="56dp"
                        android:layout_height="56dp"
                        android:background="@drawable/bg_button_speaker"
                        android:src="@android:drawable/ic_btn_speak_now"
                        android:contentDescription="Speak" />

                    <ImageButton
                        android:id="@+id/btnPlayMelody"
                        android:layout_width="56dp"
                        android:layout_height="56dp"
                        android:layout_marginStart="16dp"
                        android:background="@drawable/bg_button_play"
                        android:src="@android:drawable/ic_media_play"
                        android:contentDescription="Play Melody" />
                </LinearLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:text="Nhấn bất kỳ đâu để đóng"
                    android:textSize="12sp"
                    android:textColor="#AAAAAA" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </FrameLayout>

</FrameLayout>
