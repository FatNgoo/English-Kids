<?xml version="1.0" encoding="utf-8"?>
<!-- Ingredient Item - Child-friendly supermarket style -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="6dp"
    app:cardBackgroundColor="#FFFFFF"
    android:foreground="?attr/selectableItemBackground">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="10dp"
            android:gravity="center"
            android:background="@drawable/bg_ingredient_item">

            <!-- Ingredient Image Container -->
            <FrameLayout
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:background="@drawable/bg_ingredient_circle">
                
                <ImageView
                    android:id="@+id/iv_ingredient"
                    android:layout_width="50dp"
                    android:layout_height="50dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_food_placeholder"
                    android:scaleType="fitCenter"/>
            </FrameLayout>

            <!-- Ingredient Name -->
            <TextView
                android:id="@+id/tv_ingredient_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Tomato"
                android:textSize="13sp"
                android:textColor="#5D4037"
                android:textStyle="bold"
                android:gravity="center"
                android:layout_marginTop="8dp"
                android:maxLines="2"
                android:ellipsize="end"/>
        </LinearLayout>

        <!-- Selected Checkmark Badge -->
        <ImageView
            android:id="@+id/iv_checkmark"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="top|end"
            android:layout_margin="4dp"
            android:src="@drawable/ic_check_circle"
            android:visibility="gone"/>
    </FrameLayout>
</androidx.cardview.widget.CardView>
