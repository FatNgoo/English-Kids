<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.English" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/deep_blue</item>
        <item name="colorPrimaryDark">@color/deep_blue_dark</item>
        <item name="colorAccent">@color/shape_yellow</item>
        <item name="android:statusBarColor">@color/deep_blue_dark</item>
        <item name="android:navigationBarColor">@color/deep_blue_dark</item>
    </style>

    <style name="Theme.English" parent="Base.Theme.English" />
    
    <!-- Splash Screen Theme -->
    <style name="Theme.English.Splash" parent="Theme.English">
        <item name="android:windowBackground">@drawable/bg_splash</item>
    </style>
    
    <!-- Fullscreen Theme for Games -->
    <style name="Theme.English.Fullscreen" parent="Theme.English">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>
    
    <!-- Number Button Style for Numbers Lesson -->
    <style name="NumberButton">
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:layout_margin">6dp</item>
        <item name="android:background">@drawable/bg_number_button</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>
</resources>